#!/usr/bin/env node
/**
 * Setup script — now just verifies config and starts.
 * All data is in Supabase, no local database needed.
 */
console.log('✓ Setup complete — using Supabase for all data.');